# 500错误解决方案

## ❌ 错误现象
访问 `http://localhost:8080/api/policyKinds` 返回：
```
Whitelabel Error Page
Internal Server Error, status=500
```

## ✅ 已修复的问题

**问题根源**：MyBatisConfig配置不完整，缺少必要的配置项。

**已修复**：[MyBatisConfig.java](file:///C:/Users/苏/Desktop/河北省科技政策/test_03/src/main/java/com/example/config/MyBatisConfig.java)

### 修复前（有问题）
```java
@Bean
public SqlSessionFactoryBean sqlSessionFactory(DataSource dataSource) throws Exception {
    SqlSessionFactoryBean factory = new SqlSessionFactoryBean();
    factory.setDataSource(dataSource);
    Resource[] mapperLocations = new PathMatchingResourcePatternResolver()
            .getResources("classpath:mapper/*.xml");
    factory.setMapperLocations(mapperLocations);
    return factory;  // ❌ 返回的是Bean，不是实例
}
```

### 修复后（正确）
```java
@Bean
public SqlSessionFactory sqlSessionFactory(DataSource dataSource) throws Exception {
    SqlSessionFactoryBean factory = new SqlSessionFactoryBean();
    factory.setDataSource(dataSource);
    
    // 设置Mapper XML文件位置
    Resource[] mapperLocations = new PathMatchingResourcePatternResolver()
            .getResources("classpath:mapper/*.xml");
    factory.setMapperLocations(mapperLocations);
    
    // 设置类型别名包
    factory.setTypeAliasesPackage("com.example.entity");
    
    // 设置MyBatis配置
    org.apache.ibatis.session.Configuration configuration = new org.apache.ibatis.session.Configuration();
    configuration.setMapUnderscoreToCamelCase(true);
    factory.setConfiguration(configuration);
    
    return factory.getObject();  // ✅ 返回SqlSessionFactory实例
}
```

## 🚀 现在立即重启

### 步骤1：停止当前应用
在IDEA控制台点击红色停止按钮 ⏹️

### 步骤2：清理项目
```
IDEA菜单：Build → Clean Project
```

### 步骤3：重新构建
```
IDEA菜单：Build → Rebuild Project
```

### 步骤4：重新启动
```
右键 Test03Application.java → Run 'Test03Application'
```

## 🧪 测试API

启动成功后，依次访问：

### 1. 政策分类接口
```
http://localhost:8080/api/policyKinds
```

**预期返回**：
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "type": "综合",
      "policyCount": 10
    }
  ]
}
```

### 2. 政策列表接口
```
http://localhost:8080/api/policies
```

**预期返回**：
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [...],
    "total": 100,
    "pageNum": 1,
    "pageSize": 10,
    "totalPages": 10
  }
}
```

## 📊 如果还有500错误

### 查看后端控制台日志

在IDEA控制台中查找错误信息，通常会显示：

```
java.lang.Exception: xxx
  at com.example.xxx
  Caused by: xxx
```

**常见错误**：

#### 错误1：数据库连接失败
```
Communications link failure
```
**解决**：检查MySQL是否启动，密码是否正确

#### 错误2：表不存在
```
Table 'febs.policy' doesn't exist
```
**解决**：确认数据库中确实有policy表

#### 错误3：Mapper XML找不到
```
Invalid bound statement (not found)
```
**解决**：检查XML文件是否在 `src/main/resources/mapper/` 目录

#### 错误4：type字段为空导致查询异常
**检查**：
```sql
USE febs;
SELECT COUNT(*) FROM policy WHERE type IS NULL OR type = '';
```

## 🔍 完整配置检查清单

### ✅ MyBatisConfig.java
- [x] 返回类型改为 `SqlSessionFactory`
- [x] 添加了 `setTypeAliasesPackage`
- [x] 添加了 `setConfiguration`
- [x] 返回 `factory.getObject()`

### ✅ application.properties
```properties
server.port=8080

spring.datasource.url=jdbc:mysql://localhost:3306/febs?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.example.entity
```

### ✅ Test03Application.java
```java
@SpringBootApplication  // 只有这个注解
public class Test03Application {
    // ...
}
```

### ✅ Mapper接口
- PolicyMapper.java - 无注解
- PolicyKindMapper.java - 无注解

## 💡 为什么之前会500错误？

1. **返回类型错误**：返回 `SqlSessionFactoryBean` 而不是 `SqlSessionFactory`
2. **缺少配置**：没有设置 `typeAliasesPackage` 和 `configuration`
3. **没有调用getObject()**：Bean没有正确实例化

## ✅ 成功标志

启动后访问API，看到：

1. ✅ 返回JSON数据（不是错误页面）
2. ✅ code为200
3. ✅ data中有数据
4. ✅ 控制台无错误日志

## 📝 下一步

如果启动成功，API正常返回数据，就可以：
1. 启动前端项目
2. 测试前端是否正常显示
3. 前后端联调

---

**立即重启后端，然后访问 `http://localhost:8080/api/policyKinds` 测试！**

如果还有错误，把IDEA控制台的完整错误日志发给我！
